# Frontend

...existing code...

## Developer tooling
After running `npm install`, enable git hooks with:

npm run prepare

This project uses ESLint, Prettier and Stylelint. Run `npm run lint` and `npm run format` to check style and format code.